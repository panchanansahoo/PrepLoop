# =============================================================================
# DEPRECATED — Superseded by deploy-orchestrator-vercel-azure.yml
#   which calls deploy-backend-azure.yml → deploy-frontend-vercel.yml
#
# This stub had placeholder values and caused Rollup build failures.
# Kept as manual-only for reference; will not run on push.
# =============================================================================

name: "[Deprecated] Azure Web App CI/CD"

on:
  workflow_dispatch:

jobs:
  notice:
    runs-on: ubuntu-latest
    steps:
      - name: Deprecation notice
        run: |
          echo "This workflow is deprecated."
          echo "Use 'Deploy Orchestrator (Azure Backend -> Vercel Frontend)' instead."
          exit 0
