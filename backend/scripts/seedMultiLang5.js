import { supabaseAdmin } from '../db/supabaseClient.js';
const S = {
    // Stack problems
    71: { javascript: `function trapRainWater(heightMap) { if(!heightMap.length)return 0; const m=heightMap.length,n=heightMap[0].length; const visited=Array.from({length:m},()=>new Array(n).fill(false)); const pq=[]; const push=(r,c,h)=>{visited[r][c]=true;pq.push([h,r,c]);pq.sort((a,b)=>a[0]-b[0]);}; for(let i=0;i<m;i++){push(i,0,heightMap[i][0]);push(i,n-1,heightMap[i][n-1]);} for(let j=1;j<n-1;j++){push(0,j,heightMap[0][j]);push(m-1,j,heightMap[m-1][j]);} let water=0; const dirs=[[0,1],[0,-1],[1,0],[-1,0]]; while(pq.length){const[h,r,c]=pq.shift();for(const[dr,dc]of dirs){const nr=r+dr,nc=c+dc;if(nr>=0&&nr<m&&nc>=0&&nc<n&&!visited[nr][nc]){water+=Math.max(0,h-heightMap[nr][nc]);push(nr,nc,Math.max(h,heightMap[nr][nc]));}}} return water; }`, java: `class Solution { public int trapRainWater(int[][] h) { int m=h.length,n=h[0].length,water=0; boolean[][] v=new boolean[m][n]; PriorityQueue<int[]> pq=new PriorityQueue<>((a,b)->a[0]-b[0]); for(int i=0;i<m;i++){pq.offer(new int[]{h[i][0],i,0});v[i][0]=true;pq.offer(new int[]{h[i][n-1],i,n-1});v[i][n-1]=true;} for(int j=1;j<n-1;j++){pq.offer(new int[]{h[0][j],0,j});v[0][j]=true;pq.offer(new int[]{h[m-1][j],m-1,j});v[m-1][j]=true;} int[][] dirs={{0,1},{0,-1},{1,0},{-1,0}}; while(!pq.isEmpty()){int[] c=pq.poll();for(int[] d:dirs){int nr=c[1]+d[0],nc=c[2]+d[1];if(nr>=0&&nr<m&&nc>=0&&nc<n&&!v[nr][nc]){water+=Math.max(0,c[0]-h[nr][nc]);pq.offer(new int[]{Math.max(c[0],h[nr][nc]),nr,nc});v[nr][nc]=true;}}} return water; } }`, cpp: `class Solution { public: int trapRainWater(vector<vector<int>>& h) { int m=h.size(),n=h[0].size(),water=0; vector<vector<bool>> v(m,vector<bool>(n,false)); priority_queue<tuple<int,int,int>,vector<tuple<int,int,int>>,greater<>> pq; for(int i=0;i<m;i++){pq.push({h[i][0],i,0});v[i][0]=true;pq.push({h[i][n-1],i,n-1});v[i][n-1]=true;} for(int j=1;j<n-1;j++){pq.push({h[0][j],0,j});v[0][j]=true;pq.push({h[m-1][j],m-1,j});v[m-1][j]=true;} int dirs[][2]={{0,1},{0,-1},{1,0},{-1,0}}; while(!pq.empty()){auto[ht,r,c]=pq.top();pq.pop();for(auto&d:dirs){int nr=r+d[0],nc=c+d[1];if(nr>=0&&nr<m&&nc>=0&&nc<n&&!v[nr][nc]){water+=max(0,ht-h[nr][nc]);pq.push({max(ht,h[nr][nc]),nr,nc});v[nr][nc]=true;}}} return water; } };` },
    102: { javascript: `function maxTurbulenceSize(arr) { let inc=1,dec=1,res=1; for(let i=1;i<arr.length;i++){if(arr[i]>arr[i-1]){inc=dec+1;dec=1;}else if(arr[i]<arr[i-1]){dec=inc+1;inc=1;}else{inc=dec=1;}res=Math.max(res,inc,dec);} return res; }`, java: `class Solution { public int maxTurbulenceSize(int[] arr) { int inc=1,dec=1,res=1; for(int i=1;i<arr.length;i++){if(arr[i]>arr[i-1]){inc=dec+1;dec=1;}else if(arr[i]<arr[i-1]){dec=inc+1;inc=1;}else{inc=dec=1;}res=Math.max(res,Math.max(inc,dec));} return res; } }`, cpp: `class Solution { public: int maxTurbulenceSize(vector<int>& arr) { int inc=1,dec=1,res=1; for(int i=1;i<arr.size();i++){if(arr[i]>arr[i-1]){inc=dec+1;dec=1;}else if(arr[i]<arr[i-1]){dec=inc+1;inc=1;}else{inc=dec=1;}res=max(res,max(inc,dec));} return res; } };` },
    103: { javascript: `function equalSubstring(s, t, maxCost) { let l=0,cost=0,res=0; for(let r=0;r<s.length;r++){cost+=Math.abs(s.charCodeAt(r)-t.charCodeAt(r));while(cost>maxCost)cost-=Math.abs(s.charCodeAt(l)-t.charCodeAt(l++));res=Math.max(res,r-l+1);} return res; }`, java: `class Solution { public int equalSubstring(String s, String t, int maxCost) { int l=0,cost=0,res=0; for(int r=0;r<s.length();r++){cost+=Math.abs(s.charAt(r)-t.charAt(r));while(cost>maxCost)cost-=Math.abs(s.charAt(l)-t.charAt(l++));res=Math.max(res,r-l+1);} return res; } }`, cpp: `class Solution { public: int equalSubstring(string s, string t, int maxCost) { int l=0,cost=0,res=0; for(int r=0;r<s.size();r++){cost+=abs(s[r]-t[r]);while(cost>maxCost)cost-=abs(s[l]-t[l++]);res=max(res,r-l+1);} return res; } };` },
    105: { javascript: `function dietPlanPerformance(cal, k, lower, upper) { let pts=0,sum=0; for(let i=0;i<cal.length;i++){sum+=cal[i];if(i>=k)sum-=cal[i-k];if(i>=k-1){if(sum<lower)pts--;else if(sum>upper)pts++;}} return pts; }`, java: `class Solution { public int dietPlanPerformance(int[] cal, int k, int lower, int upper) { int pts=0,sum=0; for(int i=0;i<cal.length;i++){sum+=cal[i];if(i>=k)sum-=cal[i-k];if(i>=k-1){if(sum<lower)pts--;else if(sum>upper)pts++;}} return pts; } }`, cpp: `class Solution { public: int dietPlanPerformance(vector<int>& cal, int k, int lower, int upper) { int pts=0,sum=0; for(int i=0;i<cal.size();i++){sum+=cal[i];if(i>=k)sum-=cal[i-k];if(i>=k-1){if(sum<lower)pts--;else if(sum>upper)pts++;}} return pts; } };` },
    111: { javascript: `function longestNiceSubstring(s) { if(s.length<2)return""; for(let i=0;i<s.length;i++){if(!s.includes(s[i].toLowerCase())||!s.includes(s[i].toUpperCase())){const l=longestNiceSubstring(s.slice(0,i)),r=longestNiceSubstring(s.slice(i+1));return l.length>=r.length?l:r;}} return s; }`, java: `class Solution { public String longestNiceSubstring(String s) { if(s.length()<2)return""; for(int i=0;i<s.length();i++){char c=s.charAt(i);if(s.indexOf(Character.toLowerCase(c))<0||s.indexOf(Character.toUpperCase(c))<0){String l=longestNiceSubstring(s.substring(0,i)),r=longestNiceSubstring(s.substring(i+1));return l.length()>=r.length()?l:r;}} return s; } }`, cpp: `class Solution { public: string longestNiceSubstring(string s) { if(s.size()<2)return""; for(int i=0;i<s.size();i++){if(s.find(tolower(s[i]))==string::npos||s.find(toupper(s[i]))==string::npos){string l=longestNiceSubstring(s.substr(0,i)),r=longestNiceSubstring(s.substr(i+1));return l.size()>=r.size()?l:r;}} return s; } };` },
    112: { javascript: `function containsNearbyDuplicate(nums, k) { const m=new Map(); for(let i=0;i<nums.length;i++){if(m.has(nums[i])&&i-m.get(nums[i])<=k)return true;m.set(nums[i],i);} return false; }`, java: `class Solution { public boolean containsNearbyDuplicate(int[] nums, int k) { Map<Integer,Integer> m=new HashMap<>(); for(int i=0;i<nums.length;i++){if(m.containsKey(nums[i])&&i-m.get(nums[i])<=k)return true;m.put(nums[i],i);} return false; } }`, cpp: `class Solution { public: bool containsNearbyDuplicate(vector<int>& nums, int k) { unordered_map<int,int> m; for(int i=0;i<nums.size();i++){if(m.count(nums[i])&&i-m[nums[i]]<=k)return true;m[nums[i]]=i;} return false; } };` },
    114: { javascript: `function longestMountain(arr) { let res=0; for(let i=1;i<arr.length-1;i++){if(arr[i]>arr[i-1]&&arr[i]>arr[i+1]){let l=i,r=i;while(l>0&&arr[l-1]<arr[l])l--;while(r<arr.length-1&&arr[r]>arr[r+1])r++;res=Math.max(res,r-l+1);}} return res; }`, java: `class Solution { public int longestMountain(int[] arr) { int res=0; for(int i=1;i<arr.length-1;i++){if(arr[i]>arr[i-1]&&arr[i]>arr[i+1]){int l=i,r=i;while(l>0&&arr[l-1]<arr[l])l--;while(r<arr.length-1&&arr[r]>arr[r+1])r++;res=Math.max(res,r-l+1);}} return res; } }`, cpp: `class Solution { public: int longestMountain(vector<int>& arr) { int res=0; for(int i=1;i<(int)arr.size()-1;i++){if(arr[i]>arr[i-1]&&arr[i]>arr[i+1]){int l=i,r=i;while(l>0&&arr[l-1]<arr[l])l--;while(r<(int)arr.size()-1&&arr[r]>arr[r+1])r++;res=max(res,r-l+1);}} return res; } };` },
    117: { javascript: `function middleNode(head) { let s=head,f=head; while(f&&f.next){s=s.next;f=f.next.next;} return s; }`, java: `class Solution { public ListNode middleNode(ListNode head) { ListNode s=head,f=head; while(f!=null&&f.next!=null){s=s.next;f=f.next.next;} return s; } }`, cpp: `class Solution { public: ListNode* middleNode(ListNode* head) { auto s=head,f=head; while(f&&f->next){s=s->next;f=f->next->next;} return s; } };` },
    119: { javascript: `function detectCycle(head) { let s=head,f=head; while(f&&f.next){s=s.next;f=f.next.next;if(s===f){s=head;while(s!==f){s=s.next;f=f.next;}return s;}}return null; }`, java: `class Solution { public ListNode detectCycle(ListNode head) { ListNode s=head,f=head; while(f!=null&&f.next!=null){s=s.next;f=f.next.next;if(s==f){s=head;while(s!=f){s=s.next;f=f.next;}return s;}} return null; } }`, cpp: `class Solution { public: ListNode *detectCycle(ListNode *head) { auto s=head,f=head; while(f&&f->next){s=s->next;f=f->next->next;if(s==f){s=head;while(s!=f){s=s->next;f=f->next;}return s;}} return nullptr; } };` },
    130: { javascript: `function removeNodes(head) { if(!head)return null; head.next=removeNodes(head.next); return head.next&&head.val<head.next.val?head.next:head; }`, java: `class Solution { public ListNode removeNodes(ListNode head) { if(head==null)return null; head.next=removeNodes(head.next); return head.next!=null&&head.val<head.next.val?head.next:head; } }`, cpp: `class Solution { public: ListNode* removeNodes(ListNode* head) { if(!head)return nullptr; head->next=removeNodes(head->next); return head->next&&head->val<head->next->val?head->next:head; } };` },
    133: { javascript: `function reverseBetween(head, left, right) { const d={val:0,next:head};let prev=d;for(let i=0;i<left-1;i++)prev=prev.next;let curr=prev.next;for(let i=0;i<right-left;i++){const next=curr.next;curr.next=next.next;next.next=prev.next;prev.next=next;}return d.next; }`, java: `class Solution { public ListNode reverseBetween(ListNode head, int left, int right) { ListNode d=new ListNode(0,head),prev=d;for(int i=0;i<left-1;i++)prev=prev.next;ListNode curr=prev.next;for(int i=0;i<right-left;i++){ListNode next=curr.next;curr.next=next.next;next.next=prev.next;prev.next=next;}return d.next; } }`, cpp: `class Solution { public: ListNode* reverseBetween(ListNode* head, int left, int right) { ListNode d(0,head);auto prev=&d;for(int i=0;i<left-1;i++)prev=prev->next;auto curr=prev->next;for(int i=0;i<right-left;i++){auto next=curr->next;curr->next=next->next;next->next=prev->next;prev->next=next;}return d.next; } };` },
    134: { javascript: `function reverseKGroup(head, k) { let curr=head,count=0;while(curr&&count<k){curr=curr.next;count++;}if(count<k)return head;let prev=reverseKGroup(curr,k);curr=head;for(let i=0;i<k;i++){const next=curr.next;curr.next=prev;prev=curr;curr=next;}return prev; }`, java: `class Solution { public ListNode reverseKGroup(ListNode head, int k) { ListNode curr=head;int count=0;while(curr!=null&&count<k){curr=curr.next;count++;}if(count<k)return head;ListNode prev=reverseKGroup(curr,k);curr=head;for(int i=0;i<k;i++){ListNode next=curr.next;curr.next=prev;prev=curr;curr=next;}return prev; } }`, cpp: `class Solution { public: ListNode* reverseKGroup(ListNode* head, int k) { auto curr=head;int count=0;while(curr&&count<k){curr=curr->next;count++;}if(count<k)return head;auto prev=reverseKGroup(curr,k);curr=head;for(int i=0;i<k;i++){auto next=curr->next;curr->next=prev;prev=curr;curr=next;}return prev; } };` },
    // Stack
    164: { javascript: `function calculate(s) { const stack=[]; let num=0,sign=1; for(const c of s){if(c>='0'&&c<='9')num=num*10+(c-'0');else if(c==='+'){stack.push(sign*num);num=0;sign=1;}else if(c==='-'){stack.push(sign*num);num=0;sign=-1;}else if(c==='('){stack.push(sign);stack.push('(');sign=1;}else if(c===')'){stack.push(sign*num);num=0;let sub=0;while(stack[stack.length-1]!=='(')sub+=stack.pop();stack.pop();sub*=stack.pop();stack.push(sub);}}stack.push(sign*num);return stack.reduce((a,b)=>a+b,0); }`, java: `class Solution { public int calculate(String s) { Deque<Integer> stack=new ArrayDeque<>();int num=0,sign=1,res=0; for(char c:s.toCharArray()){if(Character.isDigit(c))num=num*10+(c-'0');else if(c=='+'){res+=sign*num;num=0;sign=1;}else if(c=='-'){res+=sign*num;num=0;sign=-1;}else if(c=='('){stack.push(res);stack.push(sign);res=0;sign=1;num=0;}else if(c==')'){res+=sign*num;num=0;res*=stack.pop();res+=stack.pop();}} return res+sign*num; } }`, cpp: `class Solution { public: int calculate(string s) { stack<int> st;int num=0,sign=1,res=0; for(char c:s){if(isdigit(c))num=num*10+(c-'0');else if(c=='+'){res+=sign*num;num=0;sign=1;}else if(c=='-'){res+=sign*num;num=0;sign=-1;}else if(c=='('){st.push(res);st.push(sign);res=0;sign=1;}else if(c==')'){res+=sign*num;num=0;res*=st.top();st.pop();res+=st.top();st.pop();}} return res+sign*num; } };` },
    165: { javascript: `function calculate(s) { const stack=[];let num=0,op='+'; for(let i=0;i<=s.length;i++){const c=i<s.length?s[i]:'+';if(c>='0'&&c<='9')num=num*10+(c-'0');else if(c!=' '){if(op==='+')stack.push(num);else if(op==='-')stack.push(-num);else if(op==='*')stack.push(stack.pop()*num);else stack.push(Math.trunc(stack.pop()/num));num=0;op=c;}} return stack.reduce((a,b)=>a+b,0); }`, java: `class Solution { public int calculate(String s) { Deque<Integer> stack=new ArrayDeque<>();int num=0;char op='+'; for(int i=0;i<=s.length();i++){char c=i<s.length()?s.charAt(i):'+';if(Character.isDigit(c))num=num*10+(c-'0');else if(c!=' '){if(op=='+')stack.push(num);else if(op=='-')stack.push(-num);else if(op=='*')stack.push(stack.pop()*num);else stack.push(stack.pop()/num);num=0;op=c;}} int res=0;for(int n:stack)res+=n;return res; } }`, cpp: `class Solution { public: int calculate(string s) { stack<int> st;int num=0;char op='+'; for(int i=0;i<=s.size();i++){char c=i<s.size()?s[i]:'+';if(isdigit(c))num=num*10+(c-'0');else if(c!=' '){if(op=='+')st.push(num);else if(op=='-')st.push(-num);else if(op=='*'){int t=st.top();st.pop();st.push(t*num);}else{int t=st.top();st.pop();st.push(t/num);}num=0;op=c;}} int res=0;while(!st.empty()){res+=st.top();st.pop();}return res; } };` },
    167: { javascript: `function simplifyPath(path) { const stack=[]; for(const p of path.split('/')){if(p==='..')stack.pop();else if(p&&p!=='.')stack.push(p);} return '/'+stack.join('/'); }`, java: `class Solution { public String simplifyPath(String path) { Deque<String> stack=new ArrayDeque<>(); for(String p:path.split("/")){if(p.equals(".."))stack.pollLast();else if(!p.isEmpty()&&!p.equals("."))stack.offerLast(p);} return "/"+String.join("/",stack); } }`, cpp: `class Solution { public: string simplifyPath(string path) { vector<string> stack; stringstream ss(path); string s; while(getline(ss,s,'/')){if(s==".."&&!stack.empty())stack.pop_back();else if(!s.empty()&&s!="."&&s!="..")stack.push_back(s);} string res; for(auto&p:stack)res+="/"+p; return res.empty()?"/":res; } };` },
    168: { javascript: `function removeDuplicates(s) { const stack=[]; for(const c of s){if(stack.length&&stack[stack.length-1]===c)stack.pop();else stack.push(c);} return stack.join(''); }`, java: `class Solution { public String removeDuplicates(String s) { StringBuilder sb=new StringBuilder(); for(char c:s.toCharArray()){if(sb.length()>0&&sb.charAt(sb.length()-1)==c)sb.deleteCharAt(sb.length()-1);else sb.append(c);} return sb.toString(); } }`, cpp: `class Solution { public: string removeDuplicates(string s) { string stack; for(char c:s){if(!stack.empty()&&stack.back()==c)stack.pop_back();else stack+=c;} return stack; } };` },
    169: { javascript: `function removeDuplicates(s, k) { const stack=[]; for(const c of s){if(stack.length&&stack[stack.length-1][0]===c){stack[stack.length-1][1]++;if(stack[stack.length-1][1]===k)stack.pop();}else stack.push([c,1]);} return stack.map(([c,n])=>c.repeat(n)).join(''); }`, java: `class Solution { public String removeDuplicates(String s, int k) { Deque<int[]> stack=new ArrayDeque<>(); for(char c:s.toCharArray()){if(!stack.isEmpty()&&stack.peek()[0]==c){stack.peek()[1]++;if(stack.peek()[1]==k)stack.pop();}else stack.push(new int[]{c,1});} StringBuilder sb=new StringBuilder(); for(int[] p:stack)for(int i=0;i<p[1];i++)sb.append((char)p[0]); return sb.reverse().toString(); } }`, cpp: `class Solution { public: string removeDuplicates(string s, int k) { vector<pair<char,int>> stack; for(char c:s){if(!stack.empty()&&stack.back().first==c){stack.back().second++;if(stack.back().second==k)stack.pop_back();}else stack.push_back({c,1});} string res; for(auto&[c,n]:stack)res+=string(n,c); return res; } };` },
    174: { javascript: `function asteroidCollision(asteroids) { const stack=[]; for(const a of asteroids){let alive=true;while(alive&&a<0&&stack.length&&stack[stack.length-1]>0){if(stack[stack.length-1]<-a)stack.pop();else if(stack[stack.length-1]===-a){stack.pop();alive=false;}else alive=false;}if(alive)stack.push(a);} return stack; }`, java: `class Solution { public int[] asteroidCollision(int[] ast) { Deque<Integer> stack=new ArrayDeque<>(); for(int a:ast){boolean alive=true;while(alive&&a<0&&!stack.isEmpty()&&stack.peek()>0){if(stack.peek()<-a)stack.pop();else if(stack.peek()==-a){stack.pop();alive=false;}else alive=false;}if(alive)stack.push(a);} return stack.stream().mapToInt(Integer::intValue).toArray(); } }`, cpp: `class Solution { public: vector<int> asteroidCollision(vector<int>& ast) { vector<int> stack; for(int a:ast){bool alive=true;while(alive&&a<0&&!stack.empty()&&stack.back()>0){if(stack.back()<-a)stack.pop_back();else if(stack.back()==-a){stack.pop_back();alive=false;}else alive=false;}if(alive)stack.push_back(a);} return stack; } };` },
    176: { javascript: `function nextGreaterElement(nums1, nums2) { const map=new Map(),stack=[]; for(const n of nums2){while(stack.length&&stack[stack.length-1]<n)map.set(stack.pop(),n);stack.push(n);} return nums1.map(n=>map.get(n)||-1); }`, java: `class Solution { public int[] nextGreaterElement(int[] nums1, int[] nums2) { Map<Integer,Integer> map=new HashMap<>(); Deque<Integer> stack=new ArrayDeque<>(); for(int n:nums2){while(!stack.isEmpty()&&stack.peek()<n)map.put(stack.pop(),n);stack.push(n);} return Arrays.stream(nums1).map(n->map.getOrDefault(n,-1)).toArray(); } }`, cpp: `class Solution { public: vector<int> nextGreaterElement(vector<int>& nums1, vector<int>& nums2) { unordered_map<int,int> map; stack<int> st; for(int n:nums2){while(!st.empty()&&st.top()<n){map[st.top()]=n;st.pop();}st.push(n);} vector<int> res; for(int n:nums1)res.push_back(map.count(n)?map[n]:-1); return res; } };` },
    178: { javascript: `function nextGreaterElement(n) { const d=[...String(n)]; let i=d.length-2; while(i>=0&&d[i]>=d[i+1])i--; if(i<0)return -1; let j=d.length-1; while(d[j]<=d[i])j--; [d[i],d[j]]=[d[j],d[i]]; const res=parseInt(d.slice(0,i+1).concat(d.slice(i+1).reverse()).join('')); return res>2**31-1?-1:res; }`, java: `class Solution { public int nextGreaterElement(int n) { char[] d=String.valueOf(n).toCharArray(); int i=d.length-2; while(i>=0&&d[i]>=d[i+1])i--; if(i<0)return -1; int j=d.length-1; while(d[j]<=d[i])j--; char t=d[i];d[i]=d[j];d[j]=t; Arrays.sort(d,i+1,d.length); long res=Long.parseLong(new String(d)); return res>Integer.MAX_VALUE?-1:(int)res; } }`, cpp: `class Solution { public: int nextGreaterElement(int n) { string d=to_string(n); int i=d.size()-2; while(i>=0&&d[i]>=d[i+1])i--; if(i<0)return -1; int j=d.size()-1; while(d[j]<=d[i])j--; swap(d[i],d[j]); reverse(d.begin()+i+1,d.end()); long res=stol(d); return res>INT_MAX?-1:res; } };` },
    179: { javascript: `function largestRectangleArea(heights) { const stack=[]; let max=0; heights.push(0); for(let i=0;i<heights.length;i++){while(stack.length&&heights[stack[stack.length-1]]>heights[i]){const h=heights[stack.pop()];const w=stack.length?i-stack[stack.length-1]-1:i;max=Math.max(max,h*w);}stack.push(i);}return max; }`, java: `class Solution { public int largestRectangleArea(int[] h) { Deque<Integer> stack=new ArrayDeque<>();int max=0; for(int i=0;i<=h.length;i++){int cur=i==h.length?0:h[i];while(!stack.isEmpty()&&h[stack.peek()]>cur){int height=h[stack.pop()];int w=stack.isEmpty()?i:i-stack.peek()-1;max=Math.max(max,height*w);}stack.push(i);} return max; } }`, cpp: `class Solution { public: int largestRectangleArea(vector<int>& h) { stack<int> st;int mx=0; h.push_back(0); for(int i=0;i<h.size();i++){while(!st.empty()&&h[st.top()]>h[i]){int ht=h[st.top()];st.pop();int w=st.empty()?i:i-st.top()-1;mx=max(mx,ht*w);}st.push(i);} return mx; } };` },
    181: { javascript: `function trap(height) { let l=0,r=height.length-1,lMax=0,rMax=0,res=0; while(l<r){if(height[l]<height[r]){lMax=Math.max(lMax,height[l]);res+=lMax-height[l];l++;}else{rMax=Math.max(rMax,height[r]);res+=rMax-height[r];r--;}} return res; }`, java: `class Solution { public int trap(int[] h) { int l=0,r=h.length-1,lm=0,rm=0,res=0; while(l<r){if(h[l]<h[r]){lm=Math.max(lm,h[l]);res+=lm-h[l];l++;}else{rm=Math.max(rm,h[r]);res+=rm-h[r];r--;}} return res; } }`, cpp: `class Solution { public: int trap(vector<int>& h) { int l=0,r=h.size()-1,lm=0,rm=0,res=0; while(l<r){if(h[l]<h[r]){lm=max(lm,h[l]);res+=lm-h[l++];}else{rm=max(rm,h[r]);res+=rm-h[r--];}} return res; } };` },
    182: { javascript: `function scoreOfParentheses(s) { const stack=[0]; for(const c of s){if(c==='(')stack.push(0);else{const v=stack.pop();stack[stack.length-1]+=Math.max(2*v,1);}} return stack[0]; }`, java: `class Solution { public int scoreOfParentheses(String s) { Deque<Integer> stack=new ArrayDeque<>();stack.push(0); for(char c:s.toCharArray()){if(c=='(')stack.push(0);else{int v=stack.pop();stack.push(stack.pop()+Math.max(2*v,1));}} return stack.pop(); } }`, cpp: `class Solution { public: int scoreOfParentheses(string s) { stack<int> st;st.push(0); for(char c:s){if(c=='(')st.push(0);else{int v=st.top();st.pop();int t=st.top();st.pop();st.push(t+max(2*v,1));}} return st.top(); } };` },
    183: { javascript: `function checkValidString(s) { let lo=0,hi=0; for(const c of s){if(c==='('){lo++;hi++;}else if(c===')'){lo=Math.max(0,lo-1);hi--;}else{lo=Math.max(0,lo-1);hi++;}if(hi<0)return false;} return lo===0; }`, java: `class Solution { public boolean checkValidString(String s) { int lo=0,hi=0; for(char c:s.toCharArray()){if(c=='('){lo++;hi++;}else if(c==')'){lo=Math.max(0,lo-1);hi--;}else{lo=Math.max(0,lo-1);hi++;}if(hi<0)return false;} return lo==0; } }`, cpp: `class Solution { public: bool checkValidString(string s) { int lo=0,hi=0; for(char c:s){if(c=='('){lo++;hi++;}else if(c==')'){lo=max(0,lo-1);hi--;}else{lo=max(0,lo-1);hi++;}if(hi<0)return false;} return lo==0; } };` },
    184: { javascript: `function minAddToMakeValid(s) { let open=0,close=0; for(const c of s){if(c==='(')open++;else if(open>0)open--;else close++;} return open+close; }`, java: `class Solution { public int minAddToMakeValid(String s) { int open=0,close=0; for(char c:s.toCharArray()){if(c=='(')open++;else if(open>0)open--;else close++;} return open+close; } }`, cpp: `class Solution { public: int minAddToMakeValid(string s) { int open=0,close=0; for(char c:s){if(c=='(')open++;else if(open>0)open--;else close++;} return open+close; } };` },
    185: { javascript: `function minRemoveToMakeValid(s) { const arr=[...s]; const stack=[]; for(let i=0;i<arr.length;i++){if(arr[i]==='(')stack.push(i);else if(arr[i]===')'){if(stack.length)stack.pop();else arr[i]='';}} for(const i of stack)arr[i]=''; return arr.join(''); }`, java: `class Solution { public String minRemoveToMakeValid(String s) { char[] arr=s.toCharArray(); Deque<Integer> stack=new ArrayDeque<>(); for(int i=0;i<arr.length;i++){if(arr[i]=='(')stack.push(i);else if(arr[i]==')'){if(!stack.isEmpty())stack.pop();else arr[i]=' ';}} while(!stack.isEmpty())arr[stack.pop()]=' '; StringBuilder sb=new StringBuilder(); for(char c:arr)if(c!=' ')sb.append(c); return sb.toString(); } }`, cpp: `class Solution { public: string minRemoveToMakeValid(string s) { stack<int> st; for(int i=0;i<s.size();i++){if(s[i]=='(')st.push(i);else if(s[i]==')'){if(!st.empty())st.pop();else s[i]='*';}} while(!st.empty()){s[st.top()]='*';st.pop();} string res; for(char c:s)if(c!='*')res+=c; return res; } };` },
    155: { javascript: `function sortedListToBST(head) { if(!head)return null; if(!head.next)return{val:head.val,left:null,right:null}; let prev=null,slow=head,fast=head; while(fast&&fast.next){prev=slow;slow=slow.next;fast=fast.next.next;} if(prev)prev.next=null; return{val:slow.val,left:sortedListToBST(head===slow?null:head),right:sortedListToBST(slow.next)}; }`, java: `class Solution { public TreeNode sortedListToBST(ListNode head) { if(head==null)return null; if(head.next==null)return new TreeNode(head.val); ListNode prev=null,slow=head,fast=head; while(fast!=null&&fast.next!=null){prev=slow;slow=slow.next;fast=fast.next.next;} if(prev!=null)prev.next=null; TreeNode root=new TreeNode(slow.val); root.left=sortedListToBST(head==slow?null:head); root.right=sortedListToBST(slow.next); return root; } }`, cpp: `class Solution { public: TreeNode* sortedListToBST(ListNode* head) { if(!head)return nullptr; if(!head->next)return new TreeNode(head->val); ListNode*prev=nullptr,*slow=head,*fast=head; while(fast&&fast->next){prev=slow;slow=slow->next;fast=fast->next->next;} if(prev)prev->next=nullptr; auto root=new TreeNode(slow->val); root->left=sortedListToBST(head==slow?nullptr:head); root->right=sortedListToBST(slow->next); return root; } };` },
    156: { javascript: `function sortList(head) { if(!head||!head.next)return head; let s=head,f=head,prev=null; while(f&&f.next){prev=s;s=s.next;f=f.next.next;} prev.next=null; const l=sortList(head),r=sortList(s); return merge(l,r); function merge(a,b){const d={next:null};let c=d;while(a&&b){if(a.val<=b.val){c.next=a;a=a.next;}else{c.next=b;b=b.next;}c=c.next;}c.next=a||b;return d.next;} }`, java: `class Solution { public ListNode sortList(ListNode head) { if(head==null||head.next==null)return head; ListNode s=head,f=head,prev=null; while(f!=null&&f.next!=null){prev=s;s=s.next;f=f.next.next;} prev.next=null; return merge(sortList(head),sortList(s)); } ListNode merge(ListNode a,ListNode b){ListNode d=new ListNode(0),c=d;while(a!=null&&b!=null){if(a.val<=b.val){c.next=a;a=a.next;}else{c.next=b;b=b.next;}c=c.next;}c.next=a!=null?a:b;return d.next;} }`, cpp: `class Solution { public: ListNode* sortList(ListNode* head) { if(!head||!head->next)return head; ListNode*s=head,*f=head,*prev=nullptr; while(f&&f->next){prev=s;s=s->next;f=f->next->next;} prev->next=nullptr; return merge(sortList(head),sortList(s)); } ListNode* merge(ListNode*a,ListNode*b){ListNode d(0);auto c=&d;while(a&&b){if(a->val<=b->val){c->next=a;a=a->next;}else{c->next=b;b=b->next;}c=c->next;}c->next=a?a:b;return d.next;} };` },
};

async function run() {
    console.log('Adding multi-language solutions - Batch 5...\n');
    let updated = 0, failed = 0;
    for (const [id, langs] of Object.entries(S)) {
        const { data: p } = await supabaseAdmin.from('problems').select('solution_code').eq('id', parseInt(id)).single();
        if (!p) continue;
        const merged = { ...(p.solution_code || {}), ...langs };
        const { error } = await supabaseAdmin.from('problems').update({ solution_code: merged }).eq('id', parseInt(id));
        if (error) { failed++; } else { updated++; }
    }
    console.log(`Done: ${updated} updated, ${failed} failed`);
}
run();
