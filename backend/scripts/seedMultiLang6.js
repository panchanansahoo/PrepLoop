import { supabaseAdmin } from '../db/supabaseClient.js';
const S = {
    // DP continued  
    158: { javascript: `function longestValidParentheses(s) { let res=0; const stack=[-1]; for(let i=0;i<s.length;i++){if(s[i]==='(')stack.push(i);else{stack.pop();if(!stack.length)stack.push(i);else res=Math.max(res,i-stack[stack.length-1]);}} return res; }`, java: `class Solution { public int longestValidParentheses(String s) { int res=0; Deque<Integer> stack=new ArrayDeque<>();stack.push(-1); for(int i=0;i<s.length();i++){if(s.charAt(i)=='(')stack.push(i);else{stack.pop();if(stack.isEmpty())stack.push(i);else res=Math.max(res,i-stack.peek());}} return res; } }`, cpp: `class Solution { public: int longestValidParentheses(string s) { int res=0; stack<int> st; st.push(-1); for(int i=0;i<s.size();i++){if(s[i]=='(')st.push(i);else{st.pop();if(st.empty())st.push(i);else res=max(res,i-st.top());}} return res; } };` },
    159: { javascript: `class MinStack { constructor(){this.stack=[];this.minStack=[];} push(val){this.stack.push(val);this.minStack.push(Math.min(val,this.minStack.length?this.minStack[this.minStack.length-1]:Infinity));} pop(){this.stack.pop();this.minStack.pop();} top(){return this.stack[this.stack.length-1];} getMin(){return this.minStack[this.minStack.length-1];} }`, java: `class MinStack { Deque<Integer> stack=new ArrayDeque<>(),minStack=new ArrayDeque<>(); public void push(int val){stack.push(val);minStack.push(Math.min(val,minStack.isEmpty()?val:minStack.peek()));} public void pop(){stack.pop();minStack.pop();} public int top(){return stack.peek();} public int getMin(){return minStack.peek();} }`, cpp: `class MinStack { stack<int> s,ms; public: void push(int val){s.push(val);ms.push(ms.empty()?val:min(val,ms.top()));} void pop(){s.pop();ms.pop();} int top(){return s.top();} int getMin(){return ms.top();} };` },
    160: { javascript: `class MaxStack { constructor(){this.stack=[];this.maxStack=[];} push(val){this.stack.push(val);this.maxStack.push(Math.max(val,this.maxStack.length?this.maxStack[this.maxStack.length-1]:-Infinity));} pop(){this.maxStack.pop();return this.stack.pop();} top(){return this.stack[this.stack.length-1];} peekMax(){return this.maxStack[this.maxStack.length-1];} }`, java: `class MaxStack { Deque<Integer> stack=new ArrayDeque<>(),maxStack=new ArrayDeque<>(); public void push(int val){stack.push(val);maxStack.push(Math.max(val,maxStack.isEmpty()?val:maxStack.peek()));} public int pop(){maxStack.pop();return stack.pop();} public int top(){return stack.peek();} public int peekMax(){return maxStack.peek();} }`, cpp: `class MaxStack { stack<int> s,ms; public: void push(int val){s.push(val);ms.push(ms.empty()?val:max(val,ms.top()));} int pop(){ms.pop();int v=s.top();s.pop();return v;} int top(){return s.top();} int peekMax(){return ms.top();} };` },
    // Tree level order variants
    240: { javascript: `function zigzagLevelOrder(root) { if(!root)return[]; const res=[],q=[root]; let leftToRight=true; while(q.length){const sz=q.length,level=[]; for(let i=0;i<sz;i++){const n=q.shift();level.push(n.val);if(n.left)q.push(n.left);if(n.right)q.push(n.right);}res.push(leftToRight?level:level.reverse());leftToRight=!leftToRight;} return res; }`, java: `class Solution { public List<List<Integer>> zigzagLevelOrder(TreeNode root) { List<List<Integer>> res=new ArrayList<>(); if(root==null)return res; Queue<TreeNode> q=new LinkedList<>();q.offer(root);boolean ltr=true; while(!q.isEmpty()){int sz=q.size();List<Integer> level=new ArrayList<>(); for(int i=0;i<sz;i++){TreeNode n=q.poll();level.add(n.val);if(n.left!=null)q.offer(n.left);if(n.right!=null)q.offer(n.right);}if(!ltr)Collections.reverse(level);res.add(level);ltr=!ltr;} return res; } }`, cpp: `class Solution { public: vector<vector<int>> zigzagLevelOrder(TreeNode* root) { vector<vector<int>> res; if(!root)return res; queue<TreeNode*> q;q.push(root);bool ltr=true; while(!q.empty()){int sz=q.size();vector<int> level; for(int i=0;i<sz;i++){auto n=q.front();q.pop();level.push_back(n->val);if(n->left)q.push(n->left);if(n->right)q.push(n->right);}if(!ltr)reverse(level.begin(),level.end());res.push_back(level);ltr=!ltr;} return res; } };` },
    241: { javascript: `function rightSideView(root) { if(!root)return[]; const res=[],q=[root]; while(q.length){const sz=q.length; for(let i=0;i<sz;i++){const n=q.shift();if(i===sz-1)res.push(n.val);if(n.left)q.push(n.left);if(n.right)q.push(n.right);}} return res; }`, java: `class Solution { public List<Integer> rightSideView(TreeNode root) { List<Integer> res=new ArrayList<>(); if(root==null)return res; Queue<TreeNode> q=new LinkedList<>();q.offer(root); while(!q.isEmpty()){int sz=q.size(); for(int i=0;i<sz;i++){TreeNode n=q.poll();if(i==sz-1)res.add(n.val);if(n.left!=null)q.offer(n.left);if(n.right!=null)q.offer(n.right);}} return res; } }`, cpp: `class Solution { public: vector<int> rightSideView(TreeNode* root) { vector<int> res; if(!root)return res; queue<TreeNode*> q;q.push(root); while(!q.empty()){int sz=q.size(); for(int i=0;i<sz;i++){auto n=q.front();q.pop();if(i==sz-1)res.push_back(n->val);if(n->left)q.push(n->left);if(n->right)q.push(n->right);}} return res; } };` },
    242: { javascript: `function levelOrderBottom(root) { if(!root)return[]; const res=[],q=[root]; while(q.length){const sz=q.length,level=[]; for(let i=0;i<sz;i++){const n=q.shift();level.push(n.val);if(n.left)q.push(n.left);if(n.right)q.push(n.right);}res.unshift(level);} return res; }`, java: `class Solution { public List<List<Integer>> levelOrderBottom(TreeNode root) { List<List<Integer>> res=new ArrayList<>(); if(root==null)return res; Queue<TreeNode> q=new LinkedList<>();q.offer(root); while(!q.isEmpty()){int sz=q.size();List<Integer> level=new ArrayList<>(); for(int i=0;i<sz;i++){TreeNode n=q.poll();level.add(n.val);if(n.left!=null)q.offer(n.left);if(n.right!=null)q.offer(n.right);}res.add(0,level);} return res; } }`, cpp: `class Solution { public: vector<vector<int>> levelOrderBottom(TreeNode* root) { vector<vector<int>> res; if(!root)return res; queue<TreeNode*> q;q.push(root); while(!q.empty()){int sz=q.size();vector<int> level; for(int i=0;i<sz;i++){auto n=q.front();q.pop();level.push_back(n->val);if(n->left)q.push(n->left);if(n->right)q.push(n->right);}res.insert(res.begin(),level);} return res; } };` },
    243: { javascript: `function averageOfLevels(root) { const res=[],q=[root]; while(q.length){const sz=q.length;let sum=0; for(let i=0;i<sz;i++){const n=q.shift();sum+=n.val;if(n.left)q.push(n.left);if(n.right)q.push(n.right);}res.push(sum/sz);} return res; }`, java: `class Solution { public List<Double> averageOfLevels(TreeNode root) { List<Double> res=new ArrayList<>(); Queue<TreeNode> q=new LinkedList<>();q.offer(root); while(!q.isEmpty()){int sz=q.size();double sum=0; for(int i=0;i<sz;i++){TreeNode n=q.poll();sum+=n.val;if(n.left!=null)q.offer(n.left);if(n.right!=null)q.offer(n.right);}res.add(sum/sz);} return res; } }`, cpp: `class Solution { public: vector<double> averageOfLevels(TreeNode* root) { vector<double> res; queue<TreeNode*> q;q.push(root); while(!q.empty()){int sz=q.size();double sum=0; for(int i=0;i<sz;i++){auto n=q.front();q.pop();sum+=n->val;if(n->left)q.push(n->left);if(n->right)q.push(n->right);}res.push_back(sum/sz);} return res; } };` },
    // More DP
    300: { javascript: `function rob(nums) { if(nums.length===1)return nums[0]; const helper=(a)=>{let p2=0,p1=0;for(const n of a){const t=Math.max(p1,p2+n);p2=p1;p1=t;}return p1;}; return Math.max(helper(nums.slice(1)),helper(nums.slice(0,-1))); }`, java: `class Solution { public int rob(int[] nums) { if(nums.length==1)return nums[0]; return Math.max(helper(nums,0,nums.length-2),helper(nums,1,nums.length-1)); } int helper(int[] nums,int lo,int hi){int p2=0,p1=0;for(int i=lo;i<=hi;i++){int t=Math.max(p1,p2+nums[i]);p2=p1;p1=t;}return p1;} }`, cpp: `class Solution { public: int rob(vector<int>& nums) { if(nums.size()==1)return nums[0]; return max(helper(nums,0,nums.size()-2),helper(nums,1,nums.size()-1)); } int helper(vector<int>& nums,int lo,int hi){int p2=0,p1=0;for(int i=lo;i<=hi;i++){int t=max(p1,p2+nums[i]);p2=p1;p1=t;}return p1;} };` },
    302: { javascript: `function coinChange(coins, amount) { const dp=new Array(amount+1).fill(amount+1);dp[0]=0; for(const c of coins)for(let i=c;i<=amount;i++)dp[i]=Math.min(dp[i],dp[i-c]+1); return dp[amount]>amount?-1:dp[amount]; }`, java: `class Solution { public int coinChange(int[] coins, int amount) { int[] dp=new int[amount+1];Arrays.fill(dp,amount+1);dp[0]=0; for(int c:coins)for(int i=c;i<=amount;i++)dp[i]=Math.min(dp[i],dp[i-c]+1); return dp[amount]>amount?-1:dp[amount]; } }`, cpp: `class Solution { public: int coinChange(vector<int>& coins, int amount) { vector<int> dp(amount+1,amount+1);dp[0]=0; for(int c:coins)for(int i=c;i<=amount;i++)dp[i]=min(dp[i],dp[i-c]+1); return dp[amount]>amount?-1:dp[amount]; } };` },
    303: { javascript: `function change(amount, coins) { const dp=new Array(amount+1).fill(0);dp[0]=1; for(const c of coins)for(let i=c;i<=amount;i++)dp[i]+=dp[i-c]; return dp[amount]; }`, java: `class Solution { public int change(int amount, int[] coins) { int[] dp=new int[amount+1];dp[0]=1; for(int c:coins)for(int i=c;i<=amount;i++)dp[i]+=dp[i-c]; return dp[amount]; } }`, cpp: `class Solution { public: int change(int amount, vector<int>& coins) { vector<int> dp(amount+1,0);dp[0]=1; for(int c:coins)for(int i=c;i<=amount;i++)dp[i]+=dp[i-c]; return dp[amount]; } };` },
    305: { javascript: `function numSquares(n) { const dp=new Array(n+1).fill(Infinity);dp[0]=0; for(let i=1;i<=n;i++)for(let j=1;j*j<=i;j++)dp[i]=Math.min(dp[i],dp[i-j*j]+1); return dp[n]; }`, java: `class Solution { public int numSquares(int n) { int[] dp=new int[n+1];Arrays.fill(dp,Integer.MAX_VALUE);dp[0]=0; for(int i=1;i<=n;i++)for(int j=1;j*j<=i;j++)dp[i]=Math.min(dp[i],dp[i-j*j]+1); return dp[n]; } }`, cpp: `class Solution { public: int numSquares(int n) { vector<int> dp(n+1,INT_MAX);dp[0]=0; for(int i=1;i<=n;i++)for(int j=1;j*j<=i;j++)dp[i]=min(dp[i],dp[i-j*j]+1); return dp[n]; } };` },
    308: { javascript: `function uniquePaths(m, n) { const dp=new Array(n).fill(1); for(let i=1;i<m;i++)for(let j=1;j<n;j++)dp[j]+=dp[j-1]; return dp[n-1]; }`, java: `class Solution { public int uniquePaths(int m, int n) { int[] dp=new int[n];Arrays.fill(dp,1); for(int i=1;i<m;i++)for(int j=1;j<n;j++)dp[j]+=dp[j-1]; return dp[n-1]; } }`, cpp: `class Solution { public: int uniquePaths(int m, int n) { vector<int> dp(n,1); for(int i=1;i<m;i++)for(int j=1;j<n;j++)dp[j]+=dp[j-1]; return dp[n-1]; } };` },
    309: { javascript: `function uniquePathsWithObstacles(grid) { const n=grid[0].length,dp=new Array(n).fill(0);dp[0]=1; for(const row of grid)for(let j=0;j<n;j++){if(row[j])dp[j]=0;else if(j>0)dp[j]+=dp[j-1];} return dp[n-1]; }`, java: `class Solution { public int uniquePathsWithObstacles(int[][] grid) { int n=grid[0].length;int[] dp=new int[n];dp[0]=1; for(int[] row:grid)for(int j=0;j<n;j++){if(row[j]==1)dp[j]=0;else if(j>0)dp[j]+=dp[j-1];} return dp[n-1]; } }`, cpp: `class Solution { public: int uniquePathsWithObstacles(vector<vector<int>>& grid) { int n=grid[0].size();vector<int> dp(n,0);dp[0]=1; for(auto& row:grid)for(int j=0;j<n;j++){if(row[j])dp[j]=0;else if(j>0)dp[j]+=dp[j-1];} return dp[n-1]; } };` },
    311: { javascript: `function minPathSum(grid) { const m=grid.length,n=grid[0].length; for(let i=1;i<m;i++)grid[i][0]+=grid[i-1][0]; for(let j=1;j<n;j++)grid[0][j]+=grid[0][j-1]; for(let i=1;i<m;i++)for(let j=1;j<n;j++)grid[i][j]+=Math.min(grid[i-1][j],grid[i][j-1]); return grid[m-1][n-1]; }`, java: `class Solution { public int minPathSum(int[][] grid) { int m=grid.length,n=grid[0].length; for(int i=1;i<m;i++)grid[i][0]+=grid[i-1][0]; for(int j=1;j<n;j++)grid[0][j]+=grid[0][j-1]; for(int i=1;i<m;i++)for(int j=1;j<n;j++)grid[i][j]+=Math.min(grid[i-1][j],grid[i][j-1]); return grid[m-1][n-1]; } }`, cpp: `class Solution { public: int minPathSum(vector<vector<int>>& grid) { int m=grid.size(),n=grid[0].size(); for(int i=1;i<m;i++)grid[i][0]+=grid[i-1][0]; for(int j=1;j<n;j++)grid[0][j]+=grid[0][j-1]; for(int i=1;i<m;i++)for(int j=1;j<n;j++)grid[i][j]+=min(grid[i-1][j],grid[i][j-1]); return grid[m-1][n-1]; } };` },
    313: { javascript: `function maximalSquare(matrix) { const m=matrix.length,n=matrix[0].length; const dp=Array.from({length:m+1},()=>new Array(n+1).fill(0)); let max=0; for(let i=1;i<=m;i++)for(let j=1;j<=n;j++)if(matrix[i-1][j-1]==='1'){dp[i][j]=Math.min(dp[i-1][j],dp[i][j-1],dp[i-1][j-1])+1;max=Math.max(max,dp[i][j]);} return max*max; }`, java: `class Solution { public int maximalSquare(char[][] matrix) { int m=matrix.length,n=matrix[0].length,max=0; int[][] dp=new int[m+1][n+1]; for(int i=1;i<=m;i++)for(int j=1;j<=n;j++)if(matrix[i-1][j-1]=='1'){dp[i][j]=Math.min(dp[i-1][j],Math.min(dp[i][j-1],dp[i-1][j-1]))+1;max=Math.max(max,dp[i][j]);} return max*max; } }`, cpp: `class Solution { public: int maximalSquare(vector<vector<char>>& matrix) { int m=matrix.size(),n=matrix[0].size(),mx=0; vector<vector<int>> dp(m+1,vector<int>(n+1,0)); for(int i=1;i<=m;i++)for(int j=1;j<=n;j++)if(matrix[i-1][j-1]=='1'){dp[i][j]=min({dp[i-1][j],dp[i][j-1],dp[i-1][j-1]})+1;mx=max(mx,dp[i][j]);} return mx*mx; } };` },
    314: { javascript: `function longestPalindrome(s) { const n=s.length; let start=0,maxLen=1; for(let i=0;i<n;i++){let l=i,r=i;while(l>=0&&r<n&&s[l]===s[r]){if(r-l+1>maxLen){start=l;maxLen=r-l+1;}l--;r++;}l=i;r=i+1;while(l>=0&&r<n&&s[l]===s[r]){if(r-l+1>maxLen){start=l;maxLen=r-l+1;}l--;r++;}} return s.substring(start,start+maxLen); }`, java: `class Solution { int start=0,maxLen=1; public String longestPalindrome(String s) { for(int i=0;i<s.length();i++){expand(s,i,i);expand(s,i,i+1);} return s.substring(start,start+maxLen); } void expand(String s,int l,int r){while(l>=0&&r<s.length()&&s.charAt(l)==s.charAt(r)){if(r-l+1>maxLen){start=l;maxLen=r-l+1;}l--;r++;}} }`, cpp: `class Solution { public: string longestPalindrome(string s) { int start=0,maxLen=1; for(int i=0;i<s.size();i++){expand(s,i,i,start,maxLen);expand(s,i,i+1,start,maxLen);} return s.substr(start,maxLen); } void expand(string&s,int l,int r,int&start,int&maxLen){while(l>=0&&r<s.size()&&s[l]==s[r]){if(r-l+1>maxLen){start=l;maxLen=r-l+1;}l--;r++;}} };` },
    316: { javascript: `function countSubstrings(s) { let count=0; for(let i=0;i<s.length;i++){let l=i,r=i;while(l>=0&&r<s.length&&s[l]===s[r]){count++;l--;r++;}l=i;r=i+1;while(l>=0&&r<s.length&&s[l]===s[r]){count++;l--;r++;}} return count; }`, java: `class Solution { public int countSubstrings(String s) { int count=0; for(int i=0;i<s.length();i++){count+=expand(s,i,i)+expand(s,i,i+1);} return count; } int expand(String s,int l,int r){int c=0;while(l>=0&&r<s.length()&&s.charAt(l)==s.charAt(r)){c++;l--;r++;}return c;} }`, cpp: `class Solution { public: int countSubstrings(string s) { int count=0; for(int i=0;i<s.size();i++){int l=i,r=i;while(l>=0&&r<s.size()&&s[l]==s[r]){count++;l--;r++;}l=i;r=i+1;while(l>=0&&r<s.size()&&s[l]==s[r]){count++;l--;r++;}} return count; } };` },
    326: { javascript: `function maxProfit(prices) { let min=Infinity,max=0; for(const p of prices){min=Math.min(min,p);max=Math.max(max,p-min);} return max; }`, java: `class Solution { public int maxProfit(int[] prices) { int min=Integer.MAX_VALUE,max=0; for(int p:prices){min=Math.min(min,p);max=Math.max(max,p-min);} return max; } }`, cpp: `class Solution { public: int maxProfit(vector<int>& prices) { int mn=INT_MAX,mx=0; for(int p:prices){mn=min(mn,p);mx=max(mx,p-mn);} return mx; } };` },
    327: { javascript: `function maxProfit(prices) { let profit=0; for(let i=1;i<prices.length;i++)if(prices[i]>prices[i-1])profit+=prices[i]-prices[i-1]; return profit; }`, java: `class Solution { public int maxProfit(int[] prices) { int profit=0; for(int i=1;i<prices.length;i++)if(prices[i]>prices[i-1])profit+=prices[i]-prices[i-1]; return profit; } }`, cpp: `class Solution { public: int maxProfit(vector<int>& prices) { int profit=0; for(int i=1;i<prices.size();i++)if(prices[i]>prices[i-1])profit+=prices[i]-prices[i-1]; return profit; } };` },
    330: { javascript: `function maxProfit(prices) { let hold=-Infinity,sold=0,rest=0; for(const p of prices){const prevSold=sold;sold=hold+p;hold=Math.max(hold,rest-p);rest=Math.max(rest,prevSold);} return Math.max(sold,rest); }`, java: `class Solution { public int maxProfit(int[] prices) { int hold=Integer.MIN_VALUE,sold=0,rest=0; for(int p:prices){int prevSold=sold;sold=hold+p;hold=Math.max(hold,rest-p);rest=Math.max(rest,prevSold);} return Math.max(sold,rest); } }`, cpp: `class Solution { public: int maxProfit(vector<int>& prices) { int hold=INT_MIN,sold=0,rest=0; for(int p:prices){int ps=sold;sold=hold+p;hold=max(hold,rest-p);rest=max(rest,ps);} return max(sold,rest); } };` },
    331: { javascript: `function maxProfit(prices, fee) { let hold=-Infinity,cash=0; for(const p of prices){const prevCash=cash;cash=Math.max(cash,hold+p-fee);hold=Math.max(hold,prevCash-p);} return cash; }`, java: `class Solution { public int maxProfit(int[] prices, int fee) { int hold=Integer.MIN_VALUE,cash=0; for(int p:prices){int pc=cash;cash=Math.max(cash,hold+p-fee);hold=Math.max(hold,pc-p);} return cash; } }`, cpp: `class Solution { public: int maxProfit(vector<int>& prices, int fee) { int hold=INT_MIN,cash=0; for(int p:prices){int pc=cash;cash=max(cash,hold+p-fee);hold=max(hold,pc-p);} return cash; } };` },
    332: { javascript: `function wordBreak(s, wordDict) { const set=new Set(wordDict),n=s.length,dp=new Array(n+1).fill(false);dp[0]=true; for(let i=1;i<=n;i++)for(let j=0;j<i;j++)if(dp[j]&&set.has(s.substring(j,i))){dp[i]=true;break;} return dp[n]; }`, java: `class Solution { public boolean wordBreak(String s, List<String> wordDict) { Set<String> set=new HashSet<>(wordDict); boolean[] dp=new boolean[s.length()+1];dp[0]=true; for(int i=1;i<=s.length();i++)for(int j=0;j<i;j++)if(dp[j]&&set.contains(s.substring(j,i))){dp[i]=true;break;} return dp[s.length()]; } }`, cpp: `class Solution { public: bool wordBreak(string s, vector<string>& wordDict) { unordered_set<string> set(wordDict.begin(),wordDict.end()); vector<bool> dp(s.size()+1,false);dp[0]=true; for(int i=1;i<=s.size();i++)for(int j=0;j<i;j++)if(dp[j]&&set.count(s.substr(j,i-j))){dp[i]=true;break;} return dp[s.size()]; } };` },
    334: { javascript: `function canPartition(nums) { const total=nums.reduce((a,b)=>a+b); if(total%2)return false; const target=total/2; const dp=new Set([0]); for(const n of nums){const next=new Set(dp);for(const s of dp)next.add(s+n);if(next.has(target))return true;dp.clear();for(const v of next)dp.add(v);} return dp.has(target); }`, java: `class Solution { public boolean canPartition(int[] nums) { int total=0;for(int n:nums)total+=n;if(total%2!=0)return false;int t=total/2; boolean[] dp=new boolean[t+1];dp[0]=true; for(int n:nums)for(int i=t;i>=n;i--)dp[i]=dp[i]||dp[i-n]; return dp[t]; } }`, cpp: `class Solution { public: bool canPartition(vector<int>& nums) { int total=accumulate(nums.begin(),nums.end(),0);if(total%2)return false;int t=total/2; vector<bool> dp(t+1,false);dp[0]=true; for(int n:nums)for(int i=t;i>=n;i--)dp[i]=dp[i]||dp[i-n]; return dp[t]; } };` },
    335: { javascript: `function findTargetSumWays(nums, target) { const dp=new Map();dp.set(0,1); for(const n of nums){const next=new Map();for(const[s,c]of dp){next.set(s+n,(next.get(s+n)||0)+c);next.set(s-n,(next.get(s-n)||0)+c);}dp.clear();for(const[k,v]of next)dp.set(k,v);} return dp.get(target)||0; }`, java: `class Solution { public int findTargetSumWays(int[] nums, int target) { Map<Integer,Integer> dp=new HashMap<>();dp.put(0,1); for(int n:nums){Map<Integer,Integer> next=new HashMap<>();for(var e:dp.entrySet()){next.merge(e.getKey()+n,e.getValue(),Integer::sum);next.merge(e.getKey()-n,e.getValue(),Integer::sum);}dp=next;} return dp.getOrDefault(target,0); } }`, cpp: `class Solution { public: int findTargetSumWays(vector<int>& nums, int target) { unordered_map<int,int> dp;dp[0]=1; for(int n:nums){unordered_map<int,int> next;for(auto&[s,c]:dp){next[s+n]+=c;next[s-n]+=c;}dp=next;} return dp[target]; } };` },
    // Greedy continued
    418: { javascript: `function eraseOverlapIntervals(intervals) { intervals.sort((a,b)=>a[1]-b[1]); let end=-Infinity,count=0; for(const[s,e]of intervals){if(s>=end)end=e;else count++;} return count; }`, java: `class Solution { public int eraseOverlapIntervals(int[][] intervals) { Arrays.sort(intervals,(a,b)->Integer.compare(a[1],b[1])); int end=Integer.MIN_VALUE,count=0; for(int[] i:intervals){if(i[0]>=end)end=i[1];else count++;} return count; } }`, cpp: `class Solution { public: int eraseOverlapIntervals(vector<vector<int>>& intervals) { sort(intervals.begin(),intervals.end(),[](auto&a,auto&b){return a[1]<b[1];}); int end=INT_MIN,count=0; for(auto&i:intervals){if(i[0]>=end)end=i[1];else count++;} return count; } };` },
    422: { javascript: `function largestNumber(nums) { const s=nums.map(String).sort((a,b)=>(b+a).localeCompare(a+b)).join(''); return s[0]==='0'?'0':s; }`, java: `class Solution { public String largestNumber(int[] nums) { String[] s=new String[nums.length]; for(int i=0;i<nums.length;i++)s[i]=String.valueOf(nums[i]); Arrays.sort(s,(a,b)->(b+a).compareTo(a+b)); if(s[0].equals("0"))return "0"; return String.join("",s); } }`, cpp: `class Solution { public: string largestNumber(vector<int>& nums) { vector<string> s; for(int n:nums)s.push_back(to_string(n)); sort(s.begin(),s.end(),[](string&a,string&b){return a+b>b+a;}); if(s[0]=="0")return "0"; string res; for(auto&x:s)res+=x; return res; } };` },
    423: { javascript: `function maximumSwap(num) { const d=[...String(num)]; const last=new Array(10).fill(-1); for(let i=0;i<d.length;i++)last[+d[i]]=i; for(let i=0;i<d.length;i++){for(let k=9;k>+d[i];k--){if(last[k]>i){[d[i],d[last[k]]]=[d[last[k]],d[i]];return+d.join('');}}} return num; }`, java: `class Solution { public int maximumSwap(int num) { char[] d=String.valueOf(num).toCharArray(); int[] last=new int[10]; for(int i=0;i<d.length;i++)last[d[i]-'0']=i; for(int i=0;i<d.length;i++){for(int k=9;k>d[i]-'0';k--){if(last[k]>i){char t=d[i];d[i]=d[last[k]];d[last[k]]=t;return Integer.parseInt(new String(d));}}} return num; } }`, cpp: `class Solution { public: int maximumSwap(int num) { string d=to_string(num); int last[10]={}; for(int i=0;i<d.size();i++)last[d[i]-'0']=i; for(int i=0;i<d.size();i++){for(int k=9;k>d[i]-'0';k--){if(last[k]>(int)i){swap(d[i],d[last[k]]);return stoi(d);}}} return num; } };` },
    428: { javascript: `function bagOfTokensScore(tokens, power) { tokens.sort((a,b)=>a-b); let l=0,r=tokens.length-1,score=0,max=0; while(l<=r){if(power>=tokens[l]){power-=tokens[l++];score++;max=Math.max(max,score);}else if(score>0){power+=tokens[r--];score--;}else break;} return max; }`, java: `class Solution { public int bagOfTokensScore(int[] tokens, int power) { Arrays.sort(tokens); int l=0,r=tokens.length-1,score=0,max=0; while(l<=r){if(power>=tokens[l]){power-=tokens[l++];score++;max=Math.max(max,score);}else if(score>0){power+=tokens[r--];score--;}else break;} return max; } }`, cpp: `class Solution { public: int bagOfTokensScore(vector<int>& tokens, int power) { sort(tokens.begin(),tokens.end()); int l=0,r=tokens.size()-1,score=0,mx=0; while(l<=r){if(power>=tokens[l]){power-=tokens[l++];score++;mx=max(mx,score);}else if(score>0){power+=tokens[r--];score--;}else break;} return mx; } };` },
    429: { javascript: `function numRescueBoats(people, limit) { people.sort((a,b)=>a-b); let l=0,r=people.length-1,boats=0; while(l<=r){if(people[l]+people[r]<=limit)l++;r--;boats++;} return boats; }`, java: `class Solution { public int numRescueBoats(int[] p, int limit) { Arrays.sort(p); int l=0,r=p.length-1,b=0; while(l<=r){if(p[l]+p[r]<=limit)l++;r--;b++;} return b; } }`, cpp: `class Solution { public: int numRescueBoats(vector<int>& p, int limit) { sort(p.begin(),p.end()); int l=0,r=p.size()-1,b=0; while(l<=r){if(p[l]+p[r]<=limit)l++;r--;b++;} return b; } };` },
    430: { javascript: `function brokenCalc(startValue, target) { let ops=0; while(target>startValue){if(target%2===0)target/=2;else target++;ops++;} return ops+startValue-target; }`, java: `class Solution { public int brokenCalc(int startValue, int target) { int ops=0; while(target>startValue){if(target%2==0)target/=2;else target++;ops++;} return ops+startValue-target; } }`, cpp: `class Solution { public: int brokenCalc(int startValue, int target) { int ops=0; while(target>startValue){if(target%2==0)target/=2;else target++;ops++;} return ops+startValue-target; } };` },
    431: { javascript: `function advantageCount(nums1, nums2) { const sorted=[...nums1].sort((a,b)=>a-b); const idx=[...Array(nums2.length).keys()].sort((a,b)=>nums2[a]-nums2[b]); const res=new Array(nums2.length); let l=0,r=sorted.length-1; for(let i=idx.length-1;i>=0;i--){if(sorted[r]>nums2[idx[i]])res[idx[i]]=sorted[r--];else res[idx[i]]=sorted[l++];} return res; }`, java: `class Solution { public int[] advantageCount(int[] nums1, int[] nums2) { int n=nums1.length; Integer[] idx=new Integer[n]; for(int i=0;i<n;i++)idx[i]=i; Arrays.sort(idx,(a,b)->nums2[a]-nums2[b]); Arrays.sort(nums1); int[] res=new int[n]; int l=0,r=n-1; for(int i=n-1;i>=0;i--){if(nums1[r]>nums2[idx[i]])res[idx[i]]=nums1[r--];else res[idx[i]]=nums1[l++];} return res; } }`, cpp: `class Solution { public: vector<int> advantageCount(vector<int>& nums1, vector<int>& nums2) { int n=nums1.size(); vector<int> idx(n); iota(idx.begin(),idx.end(),0); sort(idx.begin(),idx.end(),[&](int a,int b){return nums2[a]<nums2[b];}); sort(nums1.begin(),nums1.end()); vector<int> res(n); int l=0,r=n-1; for(int i=n-1;i>=0;i--){if(nums1[r]>nums2[idx[i]])res[idx[i]]=nums1[r--];else res[idx[i]]=nums1[l++];} return res; } };` },
    426: { javascript: `function minSetSize(arr) { const freq={}; for(const n of arr) freq[n]=(freq[n]||0)+1; const counts=Object.values(freq).sort((a,b)=>b-a); let removed=0; for(let i=0;i<counts.length;i++){removed+=counts[i];if(removed>=arr.length/2)return i+1;} return counts.length; }`, java: `class Solution { public int minSetSize(int[] arr) { Map<Integer,Integer> freq=new HashMap<>(); for(int n:arr) freq.merge(n,1,Integer::sum); List<Integer> counts=new ArrayList<>(freq.values()); counts.sort(Collections.reverseOrder()); int removed=0; for(int i=0;i<counts.size();i++){removed+=counts.get(i);if(removed>=arr.length/2)return i+1;} return counts.size(); } }`, cpp: `class Solution { public: int minSetSize(vector<int>& arr) { unordered_map<int,int> freq; for(int n:arr) freq[n]++; vector<int> counts; for(auto&[k,v]:freq) counts.push_back(v); sort(counts.rbegin(),counts.rend()); int removed=0; for(int i=0;i<counts.size();i++){removed+=counts[i];if(removed>=(int)arr.size()/2)return i+1;} return counts.size(); } };` },
    // Graph
    219: { javascript: `function countNodes(root) { if(!root)return 0; return 1+countNodes(root.left)+countNodes(root.right); }`, java: `class Solution { public int countNodes(TreeNode root) { if(root==null)return 0; return 1+countNodes(root.left)+countNodes(root.right); } }`, cpp: `class Solution { public: int countNodes(TreeNode* root) { if(!root)return 0; return 1+countNodes(root->left)+countNodes(root->right); } };` },
    231: { javascript: `function binaryTreePaths(root) { const res=[]; const dfs=(n,path)=>{if(!n.left&&!n.right){res.push(path);return;}if(n.left)dfs(n.left,path+'->'+n.left.val);if(n.right)dfs(n.right,path+'->'+n.right.val);}; dfs(root,String(root.val)); return res; }`, java: `class Solution { public List<String> binaryTreePaths(TreeNode root) { List<String> res=new ArrayList<>(); dfs(root,String.valueOf(root.val),res); return res; } void dfs(TreeNode n,String path,List<String> res){if(n.left==null&&n.right==null){res.add(path);return;}if(n.left!=null)dfs(n.left,path+"->"+n.left.val,res);if(n.right!=null)dfs(n.right,path+"->"+n.right.val,res);} }`, cpp: `class Solution { public: vector<string> binaryTreePaths(TreeNode* root) { vector<string> res; dfs(root,to_string(root->val),res); return res; } void dfs(TreeNode*n,string path,vector<string>&res){if(!n->left&&!n->right){res.push_back(path);return;}if(n->left)dfs(n->left,path+"->"+to_string(n->left->val),res);if(n->right)dfs(n->right,path+"->"+to_string(n->right->val),res);} };` },
    236: { javascript: `function sumNumbers(root) { let total=0; const dfs=(n,num)=>{num=num*10+n.val;if(!n.left&&!n.right){total+=num;return;}if(n.left)dfs(n.left,num);if(n.right)dfs(n.right,num);}; dfs(root,0); return total; }`, java: `class Solution { int total=0; public int sumNumbers(TreeNode root) { dfs(root,0); return total; } void dfs(TreeNode n,int num){num=num*10+n.val;if(n.left==null&&n.right==null){total+=num;return;}if(n.left!=null)dfs(n.left,num);if(n.right!=null)dfs(n.right,num);} }`, cpp: `class Solution { public: int sumNumbers(TreeNode* root) { return dfs(root,0); } int dfs(TreeNode*n,int num){num=num*10+n->val;if(!n->left&&!n->right)return num;int s=0;if(n->left)s+=dfs(n->left,num);if(n->right)s+=dfs(n->right,num);return s;} };` },
    238: { javascript: `function lowestCommonAncestor(root, p, q) { if(!root||root===p||root===q)return root; const l=lowestCommonAncestor(root.left,p,q),r=lowestCommonAncestor(root.right,p,q); if(l&&r)return root; return l||r; }`, java: `class Solution { public TreeNode lowestCommonAncestor(TreeNode root, TreeNode p, TreeNode q) { if(root==null||root==p||root==q)return root; TreeNode l=lowestCommonAncestor(root.left,p,q),r=lowestCommonAncestor(root.right,p,q); if(l!=null&&r!=null)return root; return l!=null?l:r; } }`, cpp: `class Solution { public: TreeNode* lowestCommonAncestor(TreeNode* root, TreeNode* p, TreeNode* q) { if(!root||root==p||root==q)return root; auto l=lowestCommonAncestor(root->left,p,q),r=lowestCommonAncestor(root->right,p,q); if(l&&r)return root; return l?l:r; } };` },
    282: { javascript: `function networkDelayTime(times, n, k) { const dist=new Array(n+1).fill(Infinity);dist[k]=0; for(let i=0;i<n-1;i++)for(const[u,v,w]of times)if(dist[u]!==Infinity&&dist[u]+w<dist[v])dist[v]=dist[u]+w; let max=0;for(let i=1;i<=n;i++){if(dist[i]===Infinity)return -1;max=Math.max(max,dist[i]);} return max; }`, java: `class Solution { public int networkDelayTime(int[][] times, int n, int k) { int[] dist=new int[n+1];Arrays.fill(dist,Integer.MAX_VALUE);dist[k]=0; for(int i=0;i<n-1;i++)for(int[] t:times)if(dist[t[0]]!=Integer.MAX_VALUE&&dist[t[0]]+t[2]<dist[t[1]])dist[t[1]]=dist[t[0]]+t[2]; int max=0;for(int i=1;i<=n;i++){if(dist[i]==Integer.MAX_VALUE)return -1;max=Math.max(max,dist[i]);} return max; } }`, cpp: `class Solution { public: int networkDelayTime(vector<vector<int>>& times, int n, int k) { vector<int> dist(n+1,INT_MAX);dist[k]=0; for(int i=0;i<n-1;i++)for(auto& t:times)if(dist[t[0]]!=INT_MAX&&dist[t[0]]+t[2]<dist[t[1]])dist[t[1]]=dist[t[0]]+t[2]; int mx=0;for(int i=1;i<=n;i++){if(dist[i]==INT_MAX)return -1;mx=max(mx,dist[i]);} return mx; } };` },
};

async function run() {
    console.log('Adding multi-language solutions - Batch 6...\n');
    let updated = 0, failed = 0;
    for (const [id, langs] of Object.entries(S)) {
        const { data: p } = await supabaseAdmin.from('problems').select('solution_code').eq('id', parseInt(id)).single();
        if (!p) continue;
        const merged = { ...(p.solution_code || {}), ...langs };
        const { error } = await supabaseAdmin.from('problems').update({ solution_code: merged }).eq('id', parseInt(id));
        if (error) { failed++; } else { updated++; }
    }
    console.log(`Done: ${updated} updated, ${failed} failed`);
}
run();
